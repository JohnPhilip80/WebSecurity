use db_bank;

CREATE TABLE users (
	user_id BIGINT PRIMARY KEY AUTO_INCREMENT, 
    login_name VARCHAR(40),
    login_password VARCHAR(40)
);

CREATE TABLE accounts (
	account_no BIGINT PRIMARY KEY AUTO_INCREMENT, 
    user_id BIGINT, 
    balance_amount DECIMAL(10,2),
	FOREIGN KEY(user_id) REFERENCES users(user_id)
);

CREATE TABLE sessions (
    session_id VARCHAR(100) PRIMARY KEY,
    user_id BIGINT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users(user_id)
);

INSERT INTO db_bank.users (login_name,login_password) VALUES ('JohnPhilip','john');
INSERT INTO db_bank.users (login_name,login_password) VALUES ('BalaMurugan','bala');
INSERT INTO db_bank.users (login_name,login_password) VALUES ('DonyFranklin','dony');
INSERT INTO db_bank.users (login_name,login_password) VALUES ('Rajesh','rajesh');
INSERT INTO db_bank.users (login_name,login_password) VALUES ('Saraswathy','sara');
INSERT INTO db_bank.users (login_name,login_password) VALUES ('Subashini','suba');	
INSERT INTO db_bank.users (login_name,login_password) VALUES ('Nikhila','niki');
INSERT INTO db_bank.users (login_name,login_password) VALUES ('AshishYadav','ashish');
